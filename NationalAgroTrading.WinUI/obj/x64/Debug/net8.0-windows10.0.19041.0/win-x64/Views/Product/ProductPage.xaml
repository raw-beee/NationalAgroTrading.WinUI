<Page
    x:Class="NationalAgroTrading.WinUI.Views.Product.ProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Padding="24">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<Grid Grid.Row="0" Margin="0,0,0,20">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<StackPanel>

				<TextBlock
                    Text="Products"
                    FontSize="28"
                    FontWeight="SemiBold"/>

				<TextBlock
                    Text="Manage products and pricing"
                    FontSize="14"
                    Opacity="0.7"
                    Margin="0,4,0,0"/>

			</StackPanel>

			<Button x:ConnectionId='5'
                Grid.Column="1"
                Content="Add Product"
                                              
                Padding="18,8"/>

		</Grid>

		<!-- Search -->
		<TextBox x:ConnectionId='2'
            x:Name="SearchTextBox"
            Grid.Row="1"
            PlaceholderText="Search product..."
                                                   
            Margin="0,0,0,16"/>

		<!-- Product List -->
		<Border
            Grid.Row="2"
            BorderThickness="1"
            CornerRadius="8">

			<ListView x:ConnectionId='3'
                x:Name="ProductListView"
                                                           >

				<ListView.Header>

					<Grid Padding="12">

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<TextBlock
                            Grid.Column="0"
                            Text="Product Name"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="1"
                            Text="Cost Price"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="2"
                            Text="Retail Price"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="3"
                            Text="Wholesale Price"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="4"
                            Text="VAT"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="5"
                            Text="HS Code"
                            FontWeight="SemiBold"/>

					</Grid>

				</ListView.Header>

				<ListView.ItemTemplate>

					<DataTemplate>

						<Grid Padding="12">

							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="2*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>

							<TextBlock
                                Grid.Column="0"
                                Text="{Binding ProductName}"/>

							<TextBlock
                                Grid.Column="1"
                                Text="{Binding CostPrice}"/>

							<TextBlock
                                Grid.Column="2"
                                Text="{Binding RetailPrice}"/>

							<TextBlock
                                Grid.Column="3"
                                Text="{Binding WholesalePrice}"/>

							<TextBlock
                                Grid.Column="4"
                                Text="{Binding VatText}"/>

							<TextBlock
                                Grid.Column="5"
                                Text="{Binding HSCode}"/>

						</Grid>

					</DataTemplate>

				</ListView.ItemTemplate>

			</ListView>

		</Border>

	</Grid>

</Page>

