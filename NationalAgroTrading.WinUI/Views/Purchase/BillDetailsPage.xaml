<Page
    x:Class="NationalAgroTrading.WinUI.Views.Purchase.BillDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid
        Padding="24"
        RowSpacing="16">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- HEADER -->

		<Grid
            Grid.Row="0"
            ColumnSpacing="20">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<StackPanel
                Spacing="4">

				<TextBlock
                    x:Name="BillNumberTextBlock"
                    FontSize="26"
                    FontWeight="SemiBold"/>

				<TextBlock
                    x:Name="CompanyTextBlock"
                    FontSize="15"/>

				<TextBlock
                    x:Name="DateTextBlock"
                    FontSize="14"
                    Opacity="0.7"/>

			</StackPanel>

			<StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8">

				<Button
                    Content="Edit Purchase"
                    Click="EditPurchaseButton_Click"/>

				<Button
                    Content="Delete Purchase"
                    Click="DeletePurchaseButton_Click"/>

				<Button
                    Content="Back"
                    Click="BackButton_Click"/>

			</StackPanel>

		</Grid>

		<!-- SEPARATOR -->

		<Border
            Grid.Row="1"
            Height="1"
            Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>

		<!-- DETAILS -->

		<ListView
            x:Name="PurchaseDetailsListView"
            Grid.Row="2"
            SelectionMode="None"
            IsItemClickEnabled="False">

			<ListView.Header>

				<Grid
                    Padding="12"
                    Background="{ThemeResource SystemControlBackgroundAltHighBrush}">

					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="2.2*"/>
						<ColumnDefinition Width="0.8*"/>
						<ColumnDefinition Width="1.1*"/>
						<ColumnDefinition Width="1.2*"/>
						<ColumnDefinition Width="1.1*"/>
						<ColumnDefinition Width="1.3*"/>
					</Grid.ColumnDefinitions>

					<TextBlock
                        Text="Product"
                        FontWeight="SemiBold"/>

					<TextBlock
                        Grid.Column="1"
                        Text="Qty"
                        FontWeight="SemiBold"/>

					<TextBlock
                        Grid.Column="2"
                        Text="Cost Price"
                        FontWeight="SemiBold"/>

					<TextBlock
                        Grid.Column="3"
                        Text="Subtotal"
                        FontWeight="SemiBold"/>

					<TextBlock
                        Grid.Column="4"
                        Text="VAT"
                        FontWeight="SemiBold"/>

					<TextBlock
                        Grid.Column="5"
                        Text="Total"
                        FontWeight="SemiBold"/>

				</Grid>

			</ListView.Header>

			<ListView.ItemTemplate>

				<DataTemplate>

					<Grid
                        Padding="12">

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="2.2*"/>
							<ColumnDefinition Width="0.8*"/>
							<ColumnDefinition Width="1.1*"/>
							<ColumnDefinition Width="1.2*"/>
							<ColumnDefinition Width="1.1*"/>
							<ColumnDefinition Width="1.3*"/>
						</Grid.ColumnDefinitions>

						<TextBlock
                            Text="{Binding ProductName}"/>

						<TextBlock
                            Grid.Column="1"
                            Text="{Binding QuantityText}"/>

						<TextBlock
                            Grid.Column="2"
                            Text="{Binding CostPriceText}"/>

						<TextBlock
                            Grid.Column="3"
                            Text="{Binding SubtotalText}"/>

						<TextBlock
                            Grid.Column="4"
                            Text="{Binding VATText}"/>

						<TextBlock
                            Grid.Column="5"
                            Text="{Binding TotalText}"
                            FontWeight="SemiBold"/>

					</Grid>

				</DataTemplate>

			</ListView.ItemTemplate>

		</ListView>

		<!-- SUMMARY -->

		<StackPanel
            Grid.Row="3"
            HorizontalAlignment="Right"
            Spacing="5"
            Margin="0,10,0,0">

			<TextBlock
                x:Name="SubtotalTextBlock"
                HorizontalAlignment="Right"/>

			<TextBlock
                x:Name="DiscountTextBlock"
                HorizontalAlignment="Right"/>

			<TextBlock
                x:Name="VATTextBlock"
                HorizontalAlignment="Right"/>

			<TextBlock
                x:Name="GrandTotalTextBlock"
                HorizontalAlignment="Right"
                FontSize="20"
                FontWeight="Bold"/>

		</StackPanel>

		<!-- FOOTER -->

		<TextBlock
            Grid.Row="4"
            Text="Double-check inventory after editing or deleting a purchase."
            Opacity="0.55"
            FontSize="12"/>

	</Grid>

</Page>