<Page
    x:Class="NationalAgroTrading.WinUI.Views.Inventory.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Padding="24">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<Grid
            Grid.Row="0"
            Margin="0,0,0,20">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<StackPanel>

				<TextBlock
                    Text="Inventory"
                    FontSize="28"
                    FontWeight="SemiBold"/>

				<TextBlock
                    Text="Manage opening and current stock"
                    FontSize="14"
                    Opacity="0.7"
                    Margin="0,4,0,0"/>

			</StackPanel>

			<Button
                Grid.Column="1"
                Content="Add Product"
                Click="AddProductButton_Click"
                Margin="0,0,8,0"
                Padding="18,8"/>

			<Button
                x:Name="EditInventoryButton"
                Grid.Column="2"
                Content="Edit Inventory"
                IsEnabled="False"
                Click="EditInventoryButton_Click"
                Padding="18,8"/>

		</Grid>

		<!-- Search -->
		<TextBox
            x:Name="SearchTextBox"
            Grid.Row="1"
            PlaceholderText="Search product..."
            TextChanged="SearchTextBox_TextChanged"
            Margin="0,0,0,16"/>

		<!-- Inventory table -->
		<Border
            Grid.Row="2"
            BorderThickness="1"
            CornerRadius="8">

			<ListView
                x:Name="InventoryListView"
                SelectionChanged="InventoryListView_SelectionChanged"
                DoubleTapped="InventoryListView_DoubleTapped">

				<ListView.Header>

					<Grid Padding="12">

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<TextBlock
                            Grid.Column="0"
                            Text="Product"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="1"
                            Text="Quantity"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="2"
                            Text="Total Stock"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="3"
                            Text="Stock Left"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="4"
                            Text="Retail Price"
                            FontWeight="SemiBold"/>

						<TextBlock
                            Grid.Column="5"
                            Text="Wholesale Price"
                            FontWeight="SemiBold"/>

					</Grid>

				</ListView.Header>

				<ListView.ItemTemplate>

					<DataTemplate>

						<Grid Padding="12">

							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="2*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>

							<TextBlock
                                Grid.Column="0"
                                Text="{Binding ProductName}"/>

							<TextBlock
                                Grid.Column="1"
                                Text="{Binding Quantity}"/>

							<TextBlock
                                Grid.Column="2"
                                Text="{Binding TotalStock}"/>

							<TextBlock
                                Grid.Column="3"
                                Text="{Binding TotalStockLeft}"/>

							<TextBlock
                                Grid.Column="4"
                                Text="{Binding RetailPrice}"/>

							<TextBlock
                                Grid.Column="5"
                                Text="{Binding WholesalePrice}"/>

						</Grid>

					</DataTemplate>

				</ListView.ItemTemplate>

			</ListView>

		</Border>

	</Grid>

</Page>